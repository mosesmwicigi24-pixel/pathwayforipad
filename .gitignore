# deps
node_modules/
.pnp.*
.yarn/*

# build outputs
dist/
build/
coverage/
*.tsbuildinfo

# turbo
.turbo/

# env & secrets (never commit — §5.10)
.env
.env.*
!.env.example

# mobile / expo
.expo/
*.keystore
ios/Pods/

# editor / os
.DS_Store
.idea/
*.log
.cursor/

# machine-local Claude Code permissions (never shared)
.claude/settings.local.json
