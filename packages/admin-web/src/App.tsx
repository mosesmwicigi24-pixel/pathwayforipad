// Shell only — screens (cohort table, reflection-review queue, relationship-tree
// editor, curriculum editors per §1.3) are added as features land.
export function App(): JSX.Element {
  return (
    <main>
      <h1>Nuru Place · Multiplier Portal</h1>
      <p>Scaffold ready. Cohort, review queue, and curriculum screens to follow.</p>
    </main>
  );
}
